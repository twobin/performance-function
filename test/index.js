import assert from 'assert';
import performanceFunction from '../src/';

describe('window performance', () => {
  it('it should show window performance log', done => {
    // performanceFunction();
    done();
  });
});
